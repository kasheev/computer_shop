@model BasaDate.Models.admin

@{
    ViewBag.Title = "Подробнее";
}

<h2>Подробнее</h2>

<div>
    <h4>Администраторы</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <p>Администратор</p>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.full_name)
        </dd>

    </dl>
</div>
<p>
    @if (User.IsInRole("admin")) {@Html.ActionLink("Изменение", "Edit", new { id = Model.id })} |
    @Html.ActionLink("Назад", "Index")
</p>
